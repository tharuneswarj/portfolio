import { ContactClient } from '../components/contact-client'

export const metadata = {
  title: 'Contact',
  description: 'Get in touch with Tharun Eswar.',
}

export default function ContactPage() {
  return <ContactClient />
}
